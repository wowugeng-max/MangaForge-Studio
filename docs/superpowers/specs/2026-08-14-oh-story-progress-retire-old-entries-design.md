# oh-story 进度反馈 + 下线旧质检入口

日期：2026-08-14  
状态：已确认（用户批准方案 C，2026-08-14）  
前置：`2026-08-14-oh-story-core-skill-shell-design.md`

## 目标

写作台里，质检/改稿/去 AI 只走「质检修订」上的三个 oh-story 按钮，并且执行时看得到在跑哪一个、已经多久。

## 进度（方案 C）

三个按钮共用一个忙态：`review` | `apply` | `deslop`。

- 正在跑的按钮转圈
- 另外两个禁用
- 摘要 pill：`审稿中 · 12s` / `改稿中 · 12s` / `去AI中 · 12s`
- 不造假阶段条；后端仍是同步 POST，不改成流式任务

## 下线的旧入口（写作台可见启动点）

- 章头主按钮「复检」，以及更多里的「再次复检 / 一键修订 / 生成修订报告」
- 右侧「正文质检」里的「复检当前版本 / 按自检修订 / 补动作等 chip」
- 编辑器「写后复检」里的「交稿质检 / 编辑报告」

章头主按钮改跟剩余真实步骤：未同步 →「同步故事状态」；已同步 →「写下一章」。

## 这次不动

生成正文、状态同步、写下一章、任务中心「复检收敛」、写作技能市场、导演/cockpit 内部推荐 key、工作流步骤文案里的「复检」字。
