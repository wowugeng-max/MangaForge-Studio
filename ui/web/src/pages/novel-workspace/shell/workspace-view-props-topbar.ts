export function buildNovelWorkspaceTopBarProps(deps: Record<string, any>) {
  const {
    activeKnowledgeJobCount,
    activeTasks,
    assertChapterSourceOperationCurrent,
    beginChapterSourceOperation,
    chapterGenerationSourceAuthority,
    chapterSourceLocallyBusy,
    flushPendingSave,
    isImmersiveShell,
    loadProjectModules,
    modelOptions,
    navigate,
    openCreativeAssistant,
    referenceSummary,
    selectedModelId,
    selectedProject,
    setChapterGenerationSourceAuthority,
    setSelectedModelId,
    setShellMode,
    setTaskCenterOpen,
    setWorkspaceArea,
    workspaceArea,
    workspaceAreaTabs,
  } = deps
  return {
    activeKnowledgeJobCount: activeKnowledgeJobCount,
    activeTasks: activeTasks,
    assertChapterSourceOperationCurrent: assertChapterSourceOperationCurrent,
    beginChapterSourceOperation: beginChapterSourceOperation,
    chapterGenerationSourceAuthority: chapterGenerationSourceAuthority,
    chapterSourceLocallyBusy: chapterSourceLocallyBusy,
    flushPendingSave: flushPendingSave,
    isImmersiveShell: isImmersiveShell,
    loadProjectModules: loadProjectModules,
    modelOptions: modelOptions,
    navigate: navigate,
    openCreativeAssistant: openCreativeAssistant,
    referenceSummary: referenceSummary,
    selectedModelId: selectedModelId,
    selectedProject: selectedProject,
    setChapterGenerationSourceAuthority: setChapterGenerationSourceAuthority,
    setSelectedModelId: setSelectedModelId,
    setShellMode: setShellMode,
    setTaskCenterOpen: setTaskCenterOpen,
    setWorkspaceArea: setWorkspaceArea,
    workspaceArea: workspaceArea,
    workspaceAreaTabs: workspaceAreaTabs,
  }
}
